# Verifier Role

## Purpose

Independently evaluate required checks and evidence.

## Inputs And Permissions

Use validation commands and CI evidence. Read-only except safe local checks.

## Output

Return `status`, `commands`, `results`, `evidence_refs`, and `blockers`.

## Terminal States

`complete`, `blocked`, or `escalated`; record results before closure.
