# Codex Plugins

Place one plugin package in each child directory. Each package owns its
`.codex-plugin/plugin.json` manifest and any bundled skills.

Plugins route Codex agents to installed tools and concise workflow guidance;
they must not duplicate a tool's validation or business logic.
