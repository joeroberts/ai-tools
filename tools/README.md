# Tools

Each child directory is an independently testable product. Keep its source,
tests, fixtures, documentation, and release metadata together.

Products must not depend on a Codex plugin to function. Plugins are optional
adoption layers under `../integrations/codex/plugins/`.
