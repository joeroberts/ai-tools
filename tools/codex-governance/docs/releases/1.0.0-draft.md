# 1.0.0 Draft Release Notes

This local draft has not been published or tagged.

It introduces the Go CLI foundation, configuration/template initialization,
offline Jira work-item validation, structured roadmap reporting, advisory
GitHub Actions governance checks, and the summary-only governed Ollama runtime.

Migration preserves legacy packet artifacts. Adoption remains a reviewed,
Jira-backed change; no automatic overwrite or remote mutation is included.
